/*!
 * HTTP 请求处理器模块
 *
 * 包含所有 HTTP 请求的处理器函数，负责：
 * - 解析请求参数
 * - 调用业务逻辑服务
 * - 构造 HTTP 响应
 *
 * # 子模块
 *
 * - `auth`: 身份验证相关的处理器（注册、登录、退出登录）
 * - `user`: 用户管理相关的处理器（用户信息、用户列表）
 */

/// 身份验证处理器
pub mod auth;

/// 用户管理处理器
pub mod user;

// 重新导出所有处理器函数，方便外部使用
pub use auth::*;
pub use user::*;
