# 环境变量配置模板文件
# 复制此文件为 .env 并修改其中的值以匹配你的环境

# 数据库配置
# PostgreSQL 连接 URL
# 格式：postgresql://用户名:密码@主机:端口/数据库名
DATABASE_URL=postgresql://postgres:password@localhost:5432/hello_rust

# Redis 连接URL
REDIS_URL=redis://localhost:6379/0

# JWT 身份验证配置
# JWT Token 签名密钥 - 生产环境中必须使用安全的随机字符串
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-please

# 服务器配置
# 服务器监听端口
PORT=3000

# 服务器监听主机地址
# 0.0.0.0 表示监听所有网络接口，127.0.0.1 表示只监听本地回环
HOST=0.0.0.0

# 日志配置
# 日志级别：trace, debug, info, warn, error
RUST_LOG=hello_rust=debug,tower_http=debug,axum::rejection=trace

# 开发环境特定配置
# 是否启用开发模式（可选）
DEVELOPMENT_MODE=true

# 数据库连接池配置（可选）
# 最大连接数
DB_MAX_CONNECTIONS=10

# 最小连接数
DB_MIN_CONNECTIONS=1

# 连接超时时间（秒）
DB_CONNECTION_TIMEOUT=30

# CORS 配置（可选）
# 允许的跨域来源，多个用逗号分隔
# CORS_ALLOWED_ORIGINS=http://localhost:3001,http://localhost:8080

# 安全配置（可选）
# 密码哈希配置
# ARGON2_MEMORY_COST=4096
# ARGON2_TIME_COST=3
# ARGON2_PARALLELISM=1
